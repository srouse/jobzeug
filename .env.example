OPENAI_API_KEY=
DATABASE_URL=
# Optional: set to true if your provider needs SSL and the URL does not already include it
# DATABASE_SSL=true

# Site-wide password gate (required for Next app; fail closed if missing)
SITE_PASSWORD=
SESSION_SECRET=
