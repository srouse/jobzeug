# Component definitions

This folder is intentionally **empty** after `ds2 init`. Add a subdirectory per component with **`design/definition.ts`** (see **`specs/003-ds2-components/contracts/component-package-layout.md`** in the DS2 repo). Bootstrap from Figma with `ds2 figma components capture`, or author definitions by hand.
