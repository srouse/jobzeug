# Session checkpoints

This folder caches Cursor plans used for Jobzeug and holds the running session log. Plan copies under `plans/` are snapshots for the repo; do not edit them by hand unless correcting a bad cache. The log is updated by the `session-checkpoint` skill at end of session.
